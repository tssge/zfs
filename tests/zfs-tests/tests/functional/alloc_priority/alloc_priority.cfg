#
# Configuration file for allocation priority tests
#
# These tests verify the allocation priority feature for ZFS vdevs.
# The feature allows users to control data allocation preferences
# across different storage tiers.
#

[DEFAULT]
pre = setup
post = cleanup
timeout = 300
tags = ['functional', 'alloc_priority']
